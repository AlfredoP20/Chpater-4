#include <stdio.h>

int main()
{
	float width; /* Ancho del rectangulo en pulgadas */
	float height; /* altura del rectangulo en pulgadas */
	float area; /* Area en pulgadas */
	float perimeter; /* Perimetro en pulgadas */

	width = 6.8;
	height = 2.3;

	area = ancho * altura;
	perimeter = ancho + ancho + altura + altura;

	printf("El área de un rectángulo con un ancho de %f y una altura de %f es: %f pulgadas \ n ", ancho, alto, área);
	printf("El perímetro de un rectángulo con un ancho de %f y una altura de %f es: %f pulgadas \ n ", ancho, alto, perímetro);

	return 0;
}
